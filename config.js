/* ----- Configuration -----
 * Assign your preferred string of characters
 * Any ASCII character is acceptable
 * Each character must appear in the string only once
 * 12 to 36 characters must be used
 * (this range will expand in future versions)
 */

var config = 'ABCDEF123456';

/* Validate your configuration by running
 * console.log(basex.validateConfig());
 * Validation will not run unless explicitly
 * called, as it would otherwise waste valuable
 * processing power. That's it!
 */

module.exports = { config };

